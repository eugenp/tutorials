import java.util.UUID;

public class DepositKiosk implements Depositor {
    @Override
    public void deposit(UUID userUUID, double amount) {
        // Execution logic of a cash deposit over a kiosk
    }
}
