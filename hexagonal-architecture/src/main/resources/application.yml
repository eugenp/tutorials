spring:
  application:
    name: hexagonal-architecture
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:/liquibase/changelog.xml
    enabled: true
server:
  port: 8083
