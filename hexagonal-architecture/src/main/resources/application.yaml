server:
  port: 8081
  error:
    include-stacktrace: never

spring:
  datasource:
    url: jdbc:h2:file:~/h2/posts_db;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    generate-ddl: false
      hibernate.ddl-auto: create
