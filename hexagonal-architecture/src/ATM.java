import java.util.UUID;

public class ATM implements Withdrawer {
    @Override
    public void withdraw(UUID userUUID, double amount) {
        // Withdraw logic of a cash withdrawal from an ATM
    }
}
