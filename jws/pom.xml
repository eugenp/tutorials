<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>com.example</groupId>
	<artifactId>jws</artifactId>
	<packaging>war</packaging>
	<version>0.0.1-SNAPSHOT</version>

	<name>jws-example</name>

	<dependencies>
		<dependency>
			<groupId>javax.jnlp</groupId>
			<artifactId>jnlp-servlet</artifactId>
			<version>7.0</version>
			<scope>system</scope>
			<systemPath>${project.basedir}/java-core-samples-lib/jnlp-servlet.jar</systemPath>
		</dependency>
		<dependency>
			<groupId>javax.jnlp</groupId>
			<artifactId>jardiff</artifactId>
			<version>7.0</version>
			<scope>system</scope>
			<systemPath>${project.basedir}/java-core-samples-lib/jardiff.jar</systemPath>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.0.2</version>

				<executions>
					<execution>
						<phase>compile</phase>
						<goals>
							<goal>jar</goal>
						</goals>
						<configuration>
							<archive>
								<manifest>
									<mainClass>com.example.Hello</mainClass>
								</manifest>
							</archive>
							<outputDirectory>${project.basedir}/target/jws</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.8</version>
				<executions>
					<execution>
						<phase>process-classes</phase>
						<configuration>
							<target name="pack200exe">
								<exec executable="C:\Program Files\Java\jdk1.8.0_91\bin\pack200">
									<arg value="${project.basedir}/target/jws/jws.jar" />
									<arg value="${project.basedir}/target/build/lib/jws.jar.pack.gz" />
								</exec>
							</target>
						</configuration>
						<goals>
							<!-- goal>run</goal-->
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>3.0.0</version>
				<executions>
					<execution>
						<phase>package</phase>
					</execution>
				</executions>
				<configuration>
					<webResources>
						<resource>
							<directory>${project.basedir}/java-core-samples-lib/</directory>
							<includes>
								<include>**/*.jar</include>
							</includes>
							<targetPath>WEB-INF/lib</targetPath>
						</resource>
						<!-- resource>
							<directory>${project.basedir}/target/build/lib/</directory>
							<includes>
								<include>**/*.pack.gz</include>
							</includes>
							<targetPath>/</targetPath>
						</resource-->
					</webResources>
				</configuration>
			</plugin>
		</plugins>

		<finalName>${project.artifactId}</finalName>
	</build>

</project>
