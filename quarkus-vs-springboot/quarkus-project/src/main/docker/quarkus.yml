version: '3.1'

services:
  db:
    image: postgres
    restart: always
    ports:
      - '5432'
    environment:
      - POSTGRES_PASSWORD=example
      - POSTGRESQL_USERNAME=example
      - POSTGRESQL_DATABASE=example

