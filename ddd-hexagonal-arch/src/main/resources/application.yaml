server:
  servlet:
    context-path: /hexagonal
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    password: example
    url: jdbc:mariadb://localhost:3306/hexagonal_arch
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: smtp.gmail.com
    password: password
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 5000
          starttls:
            enable: true
          timeout: 5000
          writetimeout: 5000
    username: username