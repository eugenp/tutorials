This page requires that 3 variables be defined before it is included.
 * NUMERATOR
 * DENOMINATOR
 * QUOTIENT

!|eg.Division|
|numerator|denominator|quotient?|
|${NUMERATOR}|${DENOMINATOR}|${QUOTIENT}|
