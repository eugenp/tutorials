server:
  port: 9009

spring:
  datasource:
    url: 'jdbc:h2:mem:testDb'
    driver-class-name: org.h2.Driver
    username: 'sa'
    password: 'password'
  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'

articles:
  popularity-threshold: 10000