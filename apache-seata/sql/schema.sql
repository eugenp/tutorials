CREATE TABLE table_a (
    id TEXT PRIMARY KEY,
    created TIMESTAMP NOT NULL
);

CREATE TABLE table_b (
    id TEXT PRIMARY KEY,
    created TIMESTAMP NOT NULL
);

CREATE TABLE table_c (
    id TEXT PRIMARY KEY,
    created TIMESTAMP NOT NULL
);