spring:
  cloud:
    gateway:
      routes:
      - id: route_with_metadata
        uri: https://httpbin.org
        
        metadata:
          optionName: "OptionValue"
          compositeObject:
            name: "value"
          iAmNumber: 1